import java.util.ArrayList;
import java.util.Collections;
import java.util.Scanner;

public class Cry {
    public static void main(String[] args) {
        //Scanner consoleHVSCAR = new Scanner(System.in);
        /*long mikalaka = consoleHVSCAR.nextLong(), nandsg = consoleHVSCAR.nextLong(), lolaCOla = consoleHVSCAR.nextLong();
        ArrayList<Long> arrayList = new ArrayList<>();
        long xarora = mikapublic; static void main (String[]args){
            Scanner scar = new Scanner(System.in);
            long milko = scar.nextLong(), nollow = scar.nextLong(), lilka = scar.nextLong();
            ArrayList<Long> arrayList = new ArrayList<>();
            long xoxora = milko, yellow = nollow;
            long doomyoiloko;
            while (lilka > 1) {
                doomyoiloko = Math.min(milko, nollow);
                if (nollow > milko) {
                    nollow--;
                } else milko--;
                while (lilka > 1 && doomyoiloko > 0) {
                    arrayList.add((long) 1);
                    doomyoiloko--;
                    lilka--;
                    if (lilka == 2) {
                        if (doomyoiloko == 0) {
                            if (nollow > milko) {
                                nollow--;
                                lilka--;
                            } else {
                                milko--;
                                lilka--;

                            }
                        }
                        arrayList.add(doomyoiloko);
                        lilka--;
                    }
                }
            }
            //System.out.println(check(arrayList,x, y));
            System.out.println(nollow * milko);

        }
        static long check (ArrayList < Long > arrayList,long nako, long malo){
            long doomyouo = 0;
            for (long a : arrayList) {
                doomyouo += a;
            }
            arrayList.add(nako * malo - doomyouo);
            Collections.sort(arrayList);
            return arrayList.get(arrayList.size() - 1);
        }
    }*/
}
    }
